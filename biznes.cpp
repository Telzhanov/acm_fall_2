#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
	 int n,s;
	 int counter=0;
	 cin>>n>>s;
	 vector<int> a;
	 for (int i=0; i<n; i++){
	 	int x;
	 	cin>>x;
	 	a.push_back(x);
	 }
	 sort(a.begin(),a.begin()+n);
	 for (int i=0; i<n; i++){
	 	if (a.at(i)<)
	 }
	 return 0;
}
